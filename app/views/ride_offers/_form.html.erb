<%= form_with(model: ride_offer, local: true) do |form| %>
  <% if ride_offer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ride_offer.errors.count, "error") %> prohibited this ride_offer from being saved:</h2>

      <ul>
      <% ride_offer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group row">
    <%= form.label :origin, class: "col-sm-2 col-form-label" %>
    <div class="col-sm-3">
      <%= form.text_field :origin, class: "form-control", required: true %>
    </div>
  </div>

  <div class="field form-group row">
    <%= form.label :destination, class: "col-sm-2 col-form-label" %>
    <div class="col-sm-3">
      <%= form.text_field :destination, class: "form-control", required: true %>
    </div>
  </div>

  <div class="field form-group row">
    <%= form.label :take_off, class: "col-sm-2 col-form-label" %>
    <div class="col-sm-3">
      <%= form.time_select :take_off, class: "form-control", required: true %>
    </div>
  </div>

  <div class="field form-group row">
    <%= form.label :no_of_people, class: "col-sm-2 col-form-label" %>
    <div class="col-sm-3">
      <%= form.number_field :no_of_people, class: "form-control", required: true %>
    </div>
  </div>

  <div class="form-group row actions">
    <div class="col-sm-10">
      <%= form.submit %>
    </div>
  </div>
<% end %>
